# Video_retrieval
Multimodal video retrieval system using OpenCLIP (ViT-B/32) and FAISS — find the most similar video in a database using an image or video clip as the query. Supports index persistence, top-k results, CLI and interactive modes. Python 3.10+.
